{
  "name": "hugetools",
  "version": "0.8.0",
  "private": true,
  "description": "HUGEtools catering SaaS deployment service",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "webhook": "node webhook.js",
    "check": "node --check server.js && node --check webhook.js && node --check yield-calculator.js && node --check site-selection-calculator.js && node --check revenue-analysis.js && node --check operations-workbench.js && node --check app.js && node --check api/calculate-yield.js && node --check api/calculate-site-selection.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "qrcode": "1.5.4"
  },
  "engines": {
    "node": ">=18"
  }
}
