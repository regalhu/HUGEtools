{
  "name": "hugetools",
  "version": "0.7.3",
  "private": true,
  "description": "HUGEtools catering SaaS deployment service",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "webhook": "node webhook.js",
    "check": "node --check server.js && node --check webhook.js && node --check yield-calculator.js && node --check app.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=18"
  }
}
