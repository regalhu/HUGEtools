# HUGEtools 餐饮经营小工具箱

这是一个面向小餐饮老板的本地优先工具箱。当前公开版不登录、不上传门店经营数据、不采集、不抓取，不连接任何平台代理接口；营运记录只保存在当前浏览器或微信本机。

## 已发布工具

- 营运工作台：汇总今日工作，记录会议纪要、每日日志、资料、培训反馈并生成每日营运简报，直接复用任务提醒和员工激励模块。
- 菜品毛利计算器：测算直接原料成本、标准材料毛利、渠道到手毛利、完全成本毛利。
- 门店损耗记录工具：轻量记录损耗金额、原因和责任归类。
- 应产率与产能计算工具：按采购重量、出成率、多层损耗、BOM 克重和最小采购单位测算最大出品份数、瓶颈原料、单份理论成本和原材料利用率。
- 食材成本与经营健康度：按库存流转法测算实际 COGS、食材成本率、毛利、Prime Cost 和理论/实际成本差异。
- 经营分析与老板简报：关联折后营业收入、到手收入、到手收入预算和菜品销售事实，输出到手率、预算达成、覆盖率、口径异常和多维汇总。
- 门店选址评估工具：按租金投入、经营假设、物业条件、商圈客流和竞争环境输出红线风险、财务指标、综合评分和谈判建议。
- 餐饮公司各职级招募文案生成：按餐饮类目、岗位职级和发布形式生成招聘信息、岗位职责、任职要求、福利亮点和排版文案。
- 团购套餐利润测算器：测算抖音、美团、大众点评团购套餐的单份利润、核销后总利润、加购回血和保本加购率。
- 种草类生成工具：按搜索关键词匹配公开热点方向，提供 10 套种草模板备选，生成差异化标题、正文、标签和图片拍摄意见指导。
- 评价类生成工具：基于真实消费信息整理评价内容、商家回复、拍摄建议和合规检查，不生成虚假评价。
- 门店任务提醒工具：离线新增、编辑、删除、启停门店任务，支持角色筛选、今日/逾期/已完成视图、周期提醒和内置餐饮任务模板。
- 员工激励系统：基于任务驱动、行为评分和积分激励，支持角色任务、执行记录、评分、积分、排行榜和晋升/奖金参考。
- 单店排班工具：生成轻量周排班表。
- 分享二维码：Web 端生成公网链接二维码，小程序端支持原生转发、复制路径，并可在服务器配置微信密钥后生成小程序码。

## 微信小程序版

仓库已包含原生微信小程序版本，目录为：

```text
miniprogram/
```

用微信开发者工具导入本仓库根目录即可，`project.config.json` 已指向 `miniprogram/`。当前小程序版已同步 15 个工具，包含营运工作台、毛利、损耗、应产率、经营健康、经营分析与老板简报、选址、招募、团购、种草、评价、任务、激励、排班和分享。

更多说明见：

```text
docs/WECHAT_MINIPROGRAM_GUIDE.md
```

## 安全边界

- 纯静态网页，无后端数据库。
- 不要求注册登录。
- 不主动采集或上传用户输入；营运记录只保存到当前浏览器或微信本机。
- 门店任务提醒数据只保存在当前浏览器 IndexedDB；localStorage 仅保存筛选视图等轻量设置。
- 门店任务提醒第一版不做云同步、短信、微信推送或账号系统。
- 员工激励系统用于连锁门店执行力管理，不替代工资核算、劳动合同、绩效制度或财务审批。
- 不包含爬虫、自动抓取、模拟登录、绕过验证码或绕过平台风控的功能。
- 微信小程序原生转发可在小程序前端使用；真正的小程序码生成属于微信服务端接口，AppSecret 只允许配置在云服务器环境变量中。
- 平台费率、佣金、配送费、推广费等默认值仅用于估算，最终以商家后台账单、合同和平台规则为准。
- 测算结果不构成财务、税务、法律、平台招商或盈利承诺。

## GitHub Pages

仓库启用 GitHub Pages 后，访问地址通常为：

```text
https://regalhu.github.io/HUGEtools/
```

## 本地使用

建议在 macOS 终端里用本地静态服务打开，这样版本记录等 `fetch` 读取逻辑也能正常工作：

```bash
python3 -m http.server 8765
```

然后访问：

```text
http://127.0.0.1:8765/
```

macOS 也可以直接双击运行：

```text
scripts/serve-macos.command
```

如果只想快速看页面，直接打开 `index.html` 也可使用主体计算功能；但部分浏览器会限制本地文件的剪贴板或 `fetch` 能力。

## SaaS 生产部署

天翼云生产环境采用 SaaS 级部署结构：

```text
用户 -> Nginx 公网 18089 -> Node 服务 127.0.0.1:18088
GitHub push -> Webhook 127.0.0.1:18090 -> /root/deploy.sh
```

服务器目录统一为：

```text
/www/hugetools
```

本地发布固定流程：

```bash
git add .
git commit -m "version update"
git push origin main
```

服务器更新：

```bash
/root/deploy.sh
```

当前天翼云目标入口固定为：

```text
http://113.249.104.188:18089/
```

完整 Nginx、systemd、Webhook、灰度发布、回滚和 SSH 安全说明见：

```text
docs/DEVOPS_RUNBOOK.md
```
