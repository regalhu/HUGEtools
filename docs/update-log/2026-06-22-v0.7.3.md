# 2026-06-22 更新记录：v0.7.3

## 版本

- 当前版本：0.7.3
- 上一版本：0.7.2
- 日期：2026-06-22
- 主题：固定 18089 生产入口与同步排障

## 变更范围

- 天翼云目标端口保持为 `18089`。
- Nginx 对外监听 `0.0.0.0:18089`，作为生产公网入口。
- Node 生产服务改为仅监听 `127.0.0.1:18088`，避免和 Nginx 抢占 `18089`。
- GitHub Webhook 服务继续监听 `127.0.0.1:18090`。
- 同步更新部署脚本、systemd 模板、Nginx 模板、服务器备忘录、DevOps 手册、版本档案和 README。

## 关键文件

- `deploy/nginx-hugetools-saas.conf`
- `deploy/systemd/hugetools.service`
- `deploy/server/bootstrap-tianyi.sh`
- `deploy/tianyi-hugetools-server-setup.sh`
- `docs/TIANYI_CLOUD_SERVER_MEMO.md`
- `docs/DEVOPS_RUNBOOK.md`
- `data/version-history.json`
- `version-archive.js`
- `README.md`

## 本地验证

- `app.js`、`slider-fix.js`、`version-archive.js`、`server.js`、`webhook.js` 需要通过 `node --check`。
- `data/version-history.json` 和 `schemas/yield-capacity.schema.json` 需要通过 JSON 校验。
- `deploy/server/root-deploy.sh`、`deploy/server/root-deploy-staging.sh`、`deploy/server/bootstrap-tianyi.sh` 和 `deploy/tianyi-hugetools-server-setup.sh` 需要通过 `bash -n`。

## 云端探测

2026-06-22 复查结果：

- `http://113.249.104.188:18089/` 返回 `200 OK`，但内容是 Nginx 默认欢迎页，不是 HUGEtools。
- `http://113.249.104.188:18089/hugetools/` 返回 `403 Forbidden`。
- `http://113.249.104.188:18089/data/version-history.json` 返回 `404 Not Found`。
- `http://113.249.104.188/`、`http://113.249.104.188/data/version-history.json`、`http://113.249.104.188:3389/health` 均超时。
- `ssh root@113.249.104.188` 卡在 `Connection timed out during banner exchange`，暂时无法进入服务器执行 `/root/deploy.sh`。

## GitHub 探测

- `https://github.com/regalhu/HUGEtools.git` 可读取远端 HEAD，当前远端 HEAD 为 `ed5df6d0c3a7f89958d9464523e9a8eca25034fe`。
- 本地 `origin` 仍使用 SSH 地址 `git@github.com:regalhu/HUGEtools.git`。
- 本机 GitHub SSH 推送通路卡在 `Connection timed out during banner exchange`，连接目标显示为 `198.18.0.14:22`。

## 部署状态

本地配置和版本记录已统一到 `0.7.3`，但云服务器尚未完成部署。恢复 SSH 后，在服务器上执行：

```bash
cd /www/hugetools
git fetch origin main --prune
git pull --ff-only origin main
/root/deploy.sh
curl -fsS http://127.0.0.1:18088/health
curl -fsS http://113.249.104.188:18089/data/version-history.json
```

## 风险与待办

- 需要先恢复天翼云 SSH banner 响应，才能确认 `/www/hugetools`、systemd、Nginx 和防火墙状态。
- 需要确认天翼云安全组放行 `18089/tcp`，并避免旧 Nginx 默认站点继续占用该端口。
- 如果仓库保持 private，服务器必须配置 GitHub Deploy Key 或最小权限 token，否则 `/root/deploy.sh` 无法拉取最新代码。
