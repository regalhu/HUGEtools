# 2026-06-29 · 0.7.8-miniapp.1 小程序工具同步

## 背景

微信开发者工具当前按 `project.config.json` 的 `miniprogramRoot: "miniprogram/"` 编译，因此 Web 端 `index.html` 已有 12 个工具并不等于小程序端已同步。此前小程序页面显示 `0.7.8-miniapp`，但 `miniprogram/pages/index/index.js` 只有 5 个工具入口。

## Web 端工具清单

1. 菜品毛利计算器
2. 门店损耗记录工具
3. 应产率与产能计算工具
4. 食材成本与经营健康度
5. 门店选址评估工具
6. 餐饮招募文案生成
7. 团购套餐利润测算器
8. 种草类生成工具
9. 评价类生成工具
10. 门店任务提醒工具
11. 员工激励系统
12. 单店排班工具

## 小程序端同步结果

- 小程序端版本：`0.7.8-miniapp.1`
- Web 端版本：`0.7.8`
- 已同步工具数量：12
- 暂未同步工具数量：0
- 新增版本记录：`miniprogram/version.json`
- 同步记录同时写入：`data/version-history.json` 的 `miniProgram` 字段

## 本次改动

- `miniprogram/pages/index/index.js` 补齐 12 个工具入口、状态字段和工具切换逻辑。
- `miniprogram/pages/index/index.wxml` 补齐应产率、经营健康度、门店选址、招募、评价、任务提醒、员工激励等面板。
- `miniprogram/utils/calc.js` 增加小程序端可直接调用的纯计算/生成函数，避免引用 Web DOM、`window`、`document` 或 IndexedDB。
- 种草类工具增加 10 套模板选择入口和差异化输出基础能力。

## 功能边界

本次先补齐小程序可预览、可输入、可计算/生成基础结果的闭环。任务提醒和员工激励目前是页面内存版，不做后台调度、云同步、微信推送或持久化存储。

## 验证

- `project.config.json` 保持 `miniprogramRoot: "miniprogram/"`。
- `miniprogram/app.json` 仍指向 `pages/index/index`。
- 小程序 JS 文件通过语法检查。
- JSON 配置和版本记录通过 JSON 校验。
